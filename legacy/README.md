# Legacy Code

These files are retained for historical reference and to avoid breaking any very old scripts that might still import them directly from the root.

The main codebase and updated logic are located in `src/transdetect/`. Please import from the `src.transdetect` package instead of using these legacy files.
